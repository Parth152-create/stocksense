spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/stocksense
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  threads:
    virtual:
      enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: onealpha457@gmail.com
    password: jpoxrbibcbkcxtyn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 4
          min-idle: 1
          max-wait: 1000ms

server:
  port: 8081

alphavantage:
  api:
    key: HXW27CL9C8V78EXL

# ── Sentry ────────────────────────────────────────────────────────────────────
sentry:
  dsn: ${SENTRY_DSN:}
  # Capture 10% of transactions for performance monitoring
  traces-sample-rate: 0.1
  # Only send events in production — set SENTRY_ENVIRONMENT in docker-compose
  environment: ${SENTRY_ENVIRONMENT:development}
  # Include Spring MVC request data in error reports
  enable-tracing: true
  # Don't log Sentry SDK internals
  logging:
    minimum-event-level: error
    minimum-breadcrumb-level: info

# ── Stripe ────────────────────────────────────────────────────────────────────
stripe:
  secret:
    key: ${STRIPE_SECRET_KEY:sk_test_placeholder}
  publishable:
    key: ${STRIPE_PUBLISHABLE_KEY:pk_test_placeholder}
  webhook:
    secret: ${STRIPE_WEBHOOK_SECRET:whsec_placeholder}

# ── Razorpay ──────────────────────────────────────────────────────────────────
razorpay:
  key:
    id: ${RAZORPAY_KEY_ID:rzp_test_placeholder}
    secret: ${RAZORPAY_KEY_SECRET:placeholder}

jwt:
  secret: "your-256-bit-secret-here-change-in-production-min-32-chars"
  expiration: 604800000

google:
  client-id: "724652717839-u1lacrp7rmj5rp2117vhnpoj26t5vgm5.apps.googleusercontent.com"

logging:
  level:
    com.stocksense: DEBUG
    com.stocksense.service.StockService: INFO

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never